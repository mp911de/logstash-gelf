Introduction [![Build Status](https://snap-ci.com/mp911de/logstash-gelf/branch/master/build_image)](https://snap-ci.com/mp911de/logstash-gelf/branch/master)
=============

${project.name} (${project.artifactId})

Provides logging to logstash using the Graylog Extended Logging Format (GELF). This implementation comes with support for three areas:

* [Java Util Logging](examples/jul.html)
* [log4j 1.2.x](examples/log4j-1.2.x.html)
* [log4j 2.x](examples/log4j-2.x.html)
* JBoss 7 (mix of Java Util Logging with log4j MDC)
* [Logback](examples/logback.html)

How to get
-------------

Maven:

    <dependency>
        <groupId>biz.paluch.logging</groupId>
        <artifactId>${project.artifactId}</artifactId>
        <version>${logstash-gelf.last.release}</version>
    </dependency>

JBoss Module Download:

    <dependency>
        <groupId>biz.paluch.logging</groupId>
        <artifactId>${project.artifactId}</artifactId>
        <version>${logstash-gelf.last.release}</version>
        <classifier>logging-module</classifier>
    </dependency>

or http://search.maven.org/remotecontent?filepath=biz/paluch/logging/${project.artifactId}/${logstash-gelf.last.release}/${project.artifactId}-${logstash-gelf.last.release}-logging-module.zip


